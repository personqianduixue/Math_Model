close all; clear all; clc;			%关闭所有图形窗口，清除工作空间所有变量，清空命令行
A1=round(-1.9);					%应用round( )函数对浮点数取整
A2=round(3.4);
B1=fix(-1.9);					%应用fix( )函数对浮点数取整
B2=fix(3.4);
C1=floor(-1.9);					%应用floor( )函数对浮点数取整
C2=floor(3.4);
D1=ceil(-1.9);			    	%应用ceil( )函数对浮点数取整
D2=ceil(3.4);
