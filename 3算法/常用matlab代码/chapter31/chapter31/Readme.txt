文件说明：

1. main.m为主程序，将该文件夹设置为MATLAB当前工作路径，运行即可。

2. data.mat为数据文件。

3. initpop_generate.m为初始种群产生函数，ismature.m为判断种群是否成熟函数，subpop_generate.m为子种群产生函数。

4. 该程序在MATLAB2012b版本下测试通过，个别函数在低版本中不存在或者调用格式有所不同，参照对应版本中的帮助文档修改即可。
